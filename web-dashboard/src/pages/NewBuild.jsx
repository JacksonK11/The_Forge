// NewBuild page — primary entry point for submitting a new agent build blueprint.
// Re-uses the existing Home page component which already implements the full build form.
export { default } from "./Home.jsx";
